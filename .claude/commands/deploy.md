# Deploy

Deploy the app to production server.

Run the deploy script which will:
1. Git add all changes
2. Git commit
3. Git push to production server
4. Server automatically builds and restarts

Execute this command:
```bash
cd ~/Documents/programmering/mr-avlamning && ./deploy.sh
```

Show the output to the user.
